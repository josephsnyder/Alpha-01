execute_process(COMMAND "@AutoHK@" @VISTA_BINARY_DIR@/Testing/@Letter@Test.ahk )

file(STRINGS @VISTA_BINARY_DIR@/Testing/Temporary/@Letter@Test.log OUTPUT REGEX "F -")
if(OUTPUT)
FOREACH(found ${OUTPUT})
message(${found})
ENDFOREACH(found ${OUTPUT})

endif(OUTPUT)

