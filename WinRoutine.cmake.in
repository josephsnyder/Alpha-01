execute_process(COMMAND "@AutoHK@" @VISTA_BINARY_DIR@/Testing/@FinalRoutine@.ahk )

file(STRINGS @Cache_Path@/@FinalRoutine@.log OUTPUT REGEX "F -")
if(OUTPUT)
FOREACH(found ${OUTPUT})
message(${found})
ENDFOREACH(found ${OUTPUT})

endif(OUTPUT)

