execute_process(COMMAND "@AutoHK@" @VISTA_BINARY_DIR@/Testing/@Letter@Test.ahk )

file(STRINGS @VISTA_BINARY_DIR@/Testing/Temporary/@Letter@Test.log OUTPUT REGEX "F -|W -|^@Letter@[0-9A-Z*]" )

if(OUTPUT OR OUTPUT2)
FOREACH(text ${OUTPUT})
message(${text})
ENDFOREACH(text ${OUTPUT})
endif(OUTPUT OR OUTPUT2)

